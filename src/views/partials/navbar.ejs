<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DevConnect | Community for Developers</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <nav>
        <div class="container">
            <a href="/" style="text-decoration: none;"><h2 class="brand-name">DevConnect</h2></a>
            <div class="nav-links">
                <a href="/">Home</a>
                <% if (locals.user) { %>
                    <a href="/dashboard">Dashboard</a>
                    <a href="/posts/create">New Post</a>
                    <a href="/auth/logout" class="btn-premium" style="padding: 0.5rem 1.2rem; margin-left: 1.5rem;">Logout</a>
                <% } else { %>
                    <a href="/auth/login">Login</a>
                    <a href="/auth/register" class="btn-premium" style="padding: 0.5rem 1.2rem; margin-left: 1.5rem;">Join Now</a>
                <% } %>
            </div>
        </div>
    </nav>
    <main class="container">
